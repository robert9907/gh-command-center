export default function Indexing({ isDark = true }) {
  return (
    <div style={{padding:"32px",color:isDark?"#fff":"#000"}}>
      <h2 style={{fontSize:24,fontWeight:700,marginBottom:8}}>⚡ Indexing</h2>
      <p style={{color:"#94a3b8"}}>IndexNow integration — migrating from File B next session.</p>
    </div>
  );
}
